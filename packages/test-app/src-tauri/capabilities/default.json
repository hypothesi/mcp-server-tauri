{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for test-app windows",
  "windows": ["main", "recording-toolbar"],
  "permissions": [
    "core:default",
    "opener:default",
    "mcp-bridge:default",
    "dialog:default"
  ]
}
